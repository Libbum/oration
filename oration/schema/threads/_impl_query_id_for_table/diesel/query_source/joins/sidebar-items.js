initSidebarItems({"struct":[["Inner",""],["Join","A query source representing the join between two tables"],["JoinOn","A query source representing the join between two tables with an explicit `ON` given. `Join` should usually be referenced instead, as all \"type safety\" traits are implemented in terms of `Join` implementing them."],["LeftOuter",""],["OnClauseWrapper",""]],"trait":[["AppendSelection","Used to ensure the sql type of `left.join(mid).join(right)` is `(Left, Mid, Right)` and not `((Left, Mid), Right)`. This needs to be separate from `TupleAppend` because we still want to keep the column lists (which are tuples) separate."],["JoinTo","Indicates that two tables can be joined without an explicit `ON` clause."]]});