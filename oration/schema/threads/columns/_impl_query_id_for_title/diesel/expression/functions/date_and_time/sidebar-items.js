initSidebarItems({"fn":[["date","Represents the SQL `DATE` function. The argument should be a Timestamp expression, and the return value will be an expression of type Date. # Examples `ignore # #[macro_use] extern crate diesel; # extern crate chrono; # include!(\\\"../../doctest_setup.rs\\\"); # use diesel::dsl::*; # # fn main() { # let connection = establish_connection(); let today: chrono::NaiveDate = diesel::select(date(now)).first(&connection).unwrap(); # }`"]],"struct":[["now","Represents the SQL `CURRENT_TIMESTAMP` constant. This is equivalent to the `NOW()` function on backends that support it."]]});